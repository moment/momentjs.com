p As with the other getters for durations, 
  code moment.duration().months()
  |  gets the months (0 - 12).
p
  code moment.duration().asMonths()
  |  gets the length of the duration in months.
p
  span.label.label-info Note:
  |  The length of a duration in months is defined as 30 days.